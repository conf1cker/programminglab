#include <stdlib.h>
#include <time.h>

int prime_num(int pn);

int main() {

	int pn = 2;
	pn = prime_num;
	int result;

		if ( pn > 1 )
			for ( int i = 1; i < pn; i++ ) {
				if ( pn % i == 0) {
					result = 1;

				return result;

				}
			}

	return 0;

}
